{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "tsc main.ts && zip -r ../lambda.zip *",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "actions-on-google": "^2.13.0"
  },
  "devDependencies": {
    "@tsconfig/node10": "^1.0.8",
    "aws-sdk": "^2.742.0"
  }
}
